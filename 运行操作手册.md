# 程序运行操作手册

> 程序位置：`D:\课程或实践报告\程序设计综合课程\formatter`
> 这份手册教你从零开始把程序跑起来，每一步都写清楚了。

---

## 零、先搞清楚两件事

### 1. 程序是干什么的

这是一个**源程序格式处理工具**，输入一个 C 语言源文件，做三件事：

| 功能 | 干什么 | 对应任务书 |
|---|---|---|
| 词法分析 | 把源代码拆成一个个"单词"（标识符、关键字、常量、运算符…） | 50% |
| 语法分析 | 检查语法对不对，生成**抽象语法树 AST** 并显示 | 40% |
| 格式化输出 | 按缩进规范重新生成一份 `.c` 文件 | 10% |

### 2. 程序有 6 个源文件，必须**一起编译**

```
main.c     主控（调度各模块）
lexer.c    词法分析
parser.c   语法分析 + 建 AST
ast.c      语法树的建立/显示/释放
printer.c  格式化输出
error.c    报错
```

⚠️ **只编译 main.c 会报错**（`undefined reference`），必须 6 个一起编译——这是任务书要求的"模块化"设计。

---

## 一、用 VS Code 运行（推荐）

### 第 1 步：打开项目文件夹

1. 打开 VS Code
2. 菜单栏 **文件 → 打开文件夹**
3. 选 `D:\课程或实践报告\程序设计综合课程\formatter`
4. 点"选择文件夹"

左边文件树应该能看到：`main.c`、`lexer.c`、`test` 文件夹等。

### 第 2 步：打开终端

按 **`Ctrl + `** `` ` ``（键盘左上角，数字 1 左边那个反引号键）

底部会弹出终端窗口，显示类似：

```
PS D:\课程或实践报告\程序设计综合课程\formatter>
```

> `PS` 开头说明是 PowerShell 终端（VS Code 默认）。这样就**不用切换终端**，直接用。

### 第 3 步：确认编译器可用

在终端里敲：

```powershell
gcc --version
```

**正常会显示：**

```
gcc.exe (x86_64-posix-seh-rev0, Built by MinGW-W64 project) 8.1.0
Copyright (C) 2018 Free Software Foundation, Inc.
```

- ✅ 显示版本号 → 继续第 4 步
- ❌ 报"不是内部或外部命令" → 看文末"常见问题"

### 第 4 步：编译（三种方式，任选一种）

#### 方式 ①：敲命令（最可靠，推荐）

```powershell
gcc -Wall -std=c11 -o formatter.exe main.c lexer.c ast.c parser.c printer.c error.c
```

**没有任何输出 = 编译成功。**（目录下会生成 `formatter.exe`）

#### 方式 ②：快捷键 `Ctrl + Shift + B`

我已经配置好了 `.vscode/tasks.json`，按 **`Ctrl + Shift + B`** 就会自动编译 6 个文件。

如果有多个任务，选 **"编译 formatter（全部 6 个源文件）"**。

#### 方式 ③：点右上角 ▶️ 按钮

- 如果你装了 **Code Runner** 插件：点右上角 ▶️（悬停显示 **Run Code**）
- 如果你装的是 **C/C++ Compile Run**：点它的 ▶️（悬停显示 **Compile & Run**）

两个插件我都在 `.vscode/settings.json` 里配置好了，会自动编译 6 个文件并运行 `test\05_full.c`。

> 不确定点哪个？**用方式 ① 或 ② 最稳**。

### 第 5 步：运行（两种模式）

#### 模式 A：词法分析（加 `-t`）

```powershell
.\formatter.exe test\04_punct.c -t
```

**你会看到两段输出：**

第一段——按出现顺序显示每个单词：
```
===== 阶段2: 单词表（按出现顺序）=====
PREPROCESS   : #include <stdio.h>
PREPROCESS   : #define N 10
INT          : int
IDENT        : main
LP           : (
...
```

第二段——**按种类编码排序**（任务书加分项）：
```
===== 加分项: 单词表（按种类编码排序）=====
IDENT        : main a b
INT_CONST    : 0 2 1
INT          : int
IF           : if
RETURN       : return
PLUS         : +
MINUS        : -
STAR         : *
EQ           : ==
NE           : !=
...
===== 排序显示结束（共 151 个单词）=====
```

#### 模式 B：语法分析 + 生成格式化文件（不加 `-t`）

```powershell
.\formatter.exe test\05_full.c
```

**你会看到：**

1. 阶段 0：源文件带行号显示
2. 阶段 3：**AST（抽象语法树）**，先根遍历带缩进
3. 提示：`格式化输出已写入 test/05_full_fmt.c`

### 第 6 步：看格式化结果

在 VS Code 左边的文件树里：

1. 点开 `test` 文件夹
2. 双击 `05_full_fmt.c`

你会看到重新排版后的代码：

```c
int add(int a, int b)
{
    return a + b;
}

int main()
{
    int i;
    while (i < 10)
    {
        sum = sum + arr[i];
        i = i + 1;
    }
    ...
}
```

**4 空格缩进、花括号对齐、运算符两侧有空格**——这就是格式化功能。

---

## 二、用命令行运行（不用 VS Code）

### 第 1 步：打开命令行

按 **`Win + R`** → 输入 `cmd` → 回车

（或者 `Win + R` → 输入 `powershell` → 回车）

### 第 2 步：进入项目目录

```cmd
D:
cd D:\课程或实践报告\程序设计综合课程\formatter
```

> 注意：要先输入 `D:` 切换到 D 盘，再 `cd` 进目录。

### 第 3 步：编译 + 运行

和 VS Code 里一样：

```cmd
gcc -Wall -std=c11 -o formatter.exe main.c lexer.c ast.c parser.c printer.c error.c
formatter.exe test\04_punct.c -t
formatter.exe test\05_full.c
```

**CMD 里不需要加 `.\` 前缀**（PowerShell 才需要）。

---

## 三、8 个测试文件（都跑一遍）

测试文件在 `test\` 文件夹里，每个负责测一类功能：

| 文件 | 测什么 | 怎么跑 |
|---|---|---|
| `00_ident.c` | 标识符、关键字 | `.\formatter.exe test\00_ident.c -t` |
| `01_variables.c` | 变量、数组、函数、if-else | `.\formatter.exe test\01_variables.c` |
| `02_int.c` | 整型常量 `123` `0123` `0x123` `123L` | `.\formatter.exe test\02_int.c -t` |
| `03_char.c` | 字符常量 `'a'` `'\n'` `'\0'` | `.\formatter.exe test\03_char.c -t` |
| `04_punct.c` | **全部运算符 + 注释 + 预处理** | `.\formatter.exe test\04_punct.c -t` |
| `05_full.c` | **全部语句类型**（最全的） | `.\formatter.exe test\05_full.c` |
| `06_float.c` | 浮点常量 `1.5` `.5` `1.` `1.5F` | `.\formatter.exe test\06_float.c -t` |
| `07_nested.c` | if 嵌套、循环嵌套、递归 | `.\formatter.exe test\07_nested.c` |

**规则**：
- 加 `-t` = 只做词法分析
- 不加 `-t` = 语法分析 + 显示 AST + 生成 `_fmt.c` 文件
- `00_ident.c` 是纯词法测试，只能用 `-t`

### 一键跑全部（复制粘贴到终端）

```powershell
.\formatter.exe test\00_ident.c -t
.\formatter.exe test\02_int.c -t
.\formatter.exe test\03_char.c -t
.\formatter.exe test\04_punct.c -t
.\formatter.exe test\06_float.c -t
.\formatter.exe test\01_variables.c
.\formatter.exe test\05_full.c
.\formatter.exe test\07_nested.c
```

---

## 四、验证格式化结果是否正确（加分操作）

格式化生成的文件是**合法的 C 代码**，可以用 gcc 编译验证：

```powershell
gcc -Wall -std=c11 -c test\05_full_fmt.c
```

**没有报错 = 格式化输出是正确的 C 代码。** 这一招验收时可以用来证明你的格式化没破坏语义。

---

## 五、常见问题

### Q1：报 `undefined reference to ...`

**原因**：只编译了 `main.c` 一个文件。

**解决**：必须把 6 个 `.c` 都写上：
```powershell
gcc -Wall -std=c11 -o formatter.exe main.c lexer.c ast.c parser.c printer.c error.c
```

### Q2：中文显示成乱码

**原因**：Windows 控制台默认 GBK 编码。

**解决**：程序里已经加了自动切换 UTF-8 的代码（`main.c` 里的 `SetConsoleOutputCP(65001)`）。如果还乱码，在终端先敲：

```powershell
chcp 65001
```

### Q3：`gcc` 不是内部或外部命令

**原因**：编译器没配环境变量。

**解决**：用 gcc 的完整路径代替 `gcc`：

```powershell
D:\mingw64\bin\gcc.exe -Wall -std=c11 -o formatter.exe main.c lexer.c ast.c parser.c printer.c error.c
```

### Q4：PowerShell 说"无法加载文件"

**解决**：程序前面加 `.\`：

```powershell
.\formatter.exe test\05_full.c
```

（PowerShell 出于安全不允许直接运行当前目录的程序，必须显式写 `.\`）

### Q5：改了代码后要重新编译吗

**要。** 每次改完 `.c` 文件，都要重新执行第 4 步的编译命令，否则运行的还是旧程序。

---

## 六、最快上手路径（赶时间就看这段）

```powershell
# 1. VS Code 打开 D:\课程或实践报告\程序设计综合课程\formatter
# 2. Ctrl + ` 开终端
# 3. 依次敲这三行：

gcc -Wall -std=c11 -o formatter.exe main.c lexer.c ast.c parser.c printer.c error.c
.\formatter.exe test\04_punct.c -t
.\formatter.exe test\05_full.c
```

看到单词表和 AST，就成功了。
