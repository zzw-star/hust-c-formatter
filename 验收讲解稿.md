# 验收讲解稿（向助教介绍程序）

> 这份稿子分「精简版（1 分钟）」和「详细版（3-5 分钟）」，都能直接照着念。
> 建议：**开场用精简版**，助教追问时再展开详细版的内容。

---

## 一、精简版（1 分钟，开场直接用）

> 老师好，我做的题目是**「基于高级语言源程序格式处理工具」**。
>
> 这个程序的功能是：**读入一个 C 语言源文件，先识别出里面所有的单词，再分析它的语法结构生成一棵抽象语法树，最后根据这棵树重新生成一份缩进规范的源代码文件。**
>
> 程序分成**三个功能板块**：
> 1. **词法分析**——把源代码拆成标识符、关键字、常量、运算符、定界符这些单词
> 2. **语法分析**——检查语法并生成抽象语法树 AST
> 3. **格式化输出**——遍历 AST，按 4 空格缩进重新生成 `.c` 文件
>
> 代码按**模块化**分成 6 个源文件：`main.c` 主控、`lexer.c` 词法、`parser.c` 语法、`ast.c` 语法树、`printer.c` 格式化、`error.c` 报错。
>
> 我现在给您演示一下。

---

## 二、详细版（分板块展开，助教追问时用）

### 板块 1：词法分析（任务书 50%）

> 词法部分我用的是**有穷自动机 DFA** 的思路，核心是 `gettoken()` 函数，**每调用一次识别一个单词**，返回这个单词的**种类编码**，单词本身的值存在全局数组 `token_text` 里。
>
> 处理顺序是：
> 1. 先过滤空白，遇到换行就让行号计数器 +1（为后面报错定位做准备）
> 2. **注释和预处理放在最前面处理**——这点很关键，否则 `/` 会被误判成除号
> 3. 然后按首字符分派：字母/下划线拼标识符再查关键字表；数字拼常量；单引号读字符常量；`=!<>&|` 试双字符运算符；其余查单字符表；都不匹配就报"非法字符"
>
> 支持识别的单词包括：标识符、10 个关键字、`int` 的三种常量形式（`123`、`0123`、`0x123` 编码相同）、`123L`（long，编码不同）、浮点常量（`1.5`、`.5`、`1.`、`1.5F`）、字符常量（`'a'`、`'\n'`）、全部双目运算符、定界符，还有注释和预处理。
>
> 另外，任务书里提到的**加分项"按种类编码排序显示"我也做了**——识别完所有单词后，按编码排序、同类去重再显示一遍，方便检查有没有漏识别的种类。

### 板块 2：语法分析 + AST（任务书 40%）

> 语法部分用的是**递归下降子程序法**，就是**每个语法成分对应一个子程序**：处理程序的、处理外部定义的、处理函数的、处理复合语句的、处理语句的、处理表达式的。
>
> 建出来的**抽象语法树用"孩子兄弟表示法"存储**——`child1` 指向第一个孩子，`child2` 指向下一个兄弟，这样一个结点可以有任意多个孩子。结点种类用**枚举 tag** 标记（一共 29 种），每种结点的属性用 **union 共用体**存放，比如标识符存名字、常量存数值、运算符存运算符编码。
>
> **表达式**这块我用的是指导书推荐的**运算符优先 + 双栈**算法——操作数栈存子树指针，运算符栈存运算符，按优先级决定是压栈还是归约建树。这借鉴了数据结构课的表达式求值，但**不求值，只建树**。
>
> 支持全部 12 种语句，还有 if 嵌套、循环嵌套、函数定义/声明/调用、数组。

### 板块 3：格式化输出（任务书 10%）

> 格式化是对 AST **先根遍历**，按我选定的 **Allman 风格**生成代码：4 空格缩进、花括号独占一行、运算符两侧加空格、顶层定义之间空一行，写到 `.c` 文件里。
>
> 这里我特别处理了一个细节：**常量的字面量原样保留**。我一开始踩过坑——只存数值再格式化输出的话，`0xFF` 会变成 `0`、`1.` 会变成 `1`，**语义被破坏**。后来我给常量结点加了一个字段存原始字面量，格式化时直接输出，现在 `0xFF` 还是 `0xFF`。
>
> 我还做了两层验证：格式化结果**再喂给程序还能解析**，而且能用 **gcc 编译通过**，证明输出是合法 C 代码。

---

## 三、边演示边讲（配合操作）

### 演示第 1 步：词法分析

> 我现在跑词法分析，命令是 `formatter.exe test\04_punct.c -t`。
>
> （指着屏幕）您看，第一段是**按出现顺序**显示每个单词——这是种类编码，这是单词自身值。第二段是**按种类编码排序**显示的，这是任务书的加分项。
>
> 特别注意这几个：`EQ ==` 是双字符运算符；`PREPROCESS #include` 是预处理；注释也被识别成 `COMMENT` 单词整段保留原文——它们会进 AST（任务书 3.3.3(5) 的要求），但不参与语法分析。

### 演示第 2 步：语法分析 + AST

> 现在跑完整流程 `formatter.exe test\05_full.c`。
>
> （指着 AST）这是生成的抽象语法树。根是 `Program`，下面挂的是各个外部定义。您看这个 `FuncDef`，它有 4 棵子树：返回类型、函数名、参数列表、函数体。这个 `IfElse` 有 3 棵子树：条件、then 分支、else 分支——和源文件的结构是一一对应的。

### 演示第 3 步：格式化输出

> 程序提示"格式化输出已写入 `05_full_fmt.c`"，我打开给您看。
>
> （指着文件）4 空格缩进、花括号对齐、运算符两侧有空格，比原来的排版规范多了。而且常量的写法没变——`0xFF` 还是 `0xFF`。

### 演示第 4 步（主动加分）

> 对了老师，我可以再演示一下**报错功能**。任务书说由您随机修改测试文件，我主动试一个——（删掉某个分号）您看，程序报"语法错误 第 X 行：变量定义缺少分号"，**行号是准确的**。

---

## 四、收尾：特色与不足（如果助教问）

> **特色方面**，我觉得有几点：
> 1. 常量字面量**原样保留**，格式化不改变语义
> 2. 报错位置**精确到行**——我专门修了一个问题：第 1 行少分号原本会报第 2 行，我用"上一个单词的行号"修正了
> 3. 表达式用"双栈 + 因子递归"混合，括号、函数调用、数组下标处理得更清晰
> 4. 做了单词按种类编码排序显示（加分项）
>
> **不足方面**：目前只支持任务书要求的语言子集，不支持指针、结构体；没有单目运算符（任务书明确排除的）；只做语法分析没有语义检查；预处理只识别不做宏展开。如果要继续完善，下一步可以做宏替换和类型检查。

---

## 五、常见问题衔接（助教追问时的过渡句）

| 助教问 | 你这样接 |
|---|---|
| "你这个词法怎么做的？" | "用的是有穷自动机 DFA 的思路……"（接板块 1） |
| "语法用什么方法？" | "递归下降子程序法，每个语法成分一个子程序……"（接板块 2） |
| "优先级怎么处理的？" | "运算符优先加双栈，操作数栈和运算符栈……" |
| "树怎么存的？" | "孩子兄弟表示法，加 union 共用体……" |
| "怎么证明格式化是对的？" | "两层验证：往返能解析 + gcc 能编译通过" |
| "有什么创新？" | "常量字面量原样保留、报错行号精确……" |

---

## 六、讲解时的三个提醒

1. **语速别太快**，每演示完一个步骤停一下，让助教看屏幕
2. **指着屏幕说**——指着具体那行输出讲，比空口说有说服力
3. **主动说踩过的坑**（比如 `0xFF` 变 `0`）——老师最喜欢听真实的调试过程，这比背概念加分

---

## 附：完整问答清单

如果助教问得更细，看同目录下的 **`验收问答准备.md`**（26 个问题 + 答案，含词法/语法/AST/格式化/测试/特色六大类）。
