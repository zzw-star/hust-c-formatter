# 高级语言源程序格式处理工具 — 验收指南

> 本文件教你：验收前怎么自己测一遍 + 验收现场怎么操作演示。

---

## 一、编译程序（每次改代码后第一步）

打开终端，进入 `formatter` 目录，执行：

```bash
gcc -Wall -std=c11 -o formatter.exe main.c lexer.c ast.c parser.c printer.c error.c
```

**看到没有任何输出（零警告零错误）就是编译成功。** 生成 `formatter.exe`。

---

## 二、程序有三种运行方式

| 命令 | 干什么 | 验收要不要演示 |
|---|---|---|
| `formatter.exe 文件.c -t` | 词法分析：显示单词表（按出现顺序 + 按种类排序） | ✅ 要 |
| `formatter.exe 文件.c` | 语法分析 → 显示 AST → 生成格式化文件 `文件_fmt.c` | ✅ 要 |
| `formatter.exe 文件.c -p` | 表达式单独测试（调试用） | ❌ 不用 |

---

## 三、测试集清单（test/ 目录下 8 个文件）

| 文件 | 测什么 | 对应任务书要求 |
|---|---|---|
| `00_ident.c` | 标识符、关键字 | 词法：标识符+关键字 |
| `01_variables.c` | 变量、数组、函数声明/定义、if-else、函数调用 | 语法基础 |
| `02_int.c` | 整型常量：`123` `0123` `0x123` `0xABC` `123L` | 词法：int 三种形式 + long |
| `03_char.c` | 字符常量：`'a'` `'X'` `'\n'` `'\0'` | 词法：字符常量 |
| `04_punct.c` | 全部运算符 + 定界符 + 注释 + 预处理 | 词法：运算符/定界符/注释/预处理 |
| `05_full.c` | 全部语句 + 函数 + 数组 + 嵌套 | 语法：全语句覆盖 |
| `06_float.c` | 浮点常量：`1.5` `.5` `1.` `1.5F` | 词法：浮点常量 |
| `07_nested.c` | if 嵌套 + 双重循环嵌套 + 递归调用 | 语法：嵌套 |

---

## 四、验收前自测（自己完整测一遍）

### 第 1 步：编译

```bash
gcc -Wall -std=c11 -o formatter.exe main.c lexer.c ast.c parser.c printer.c error.c
```

### 第 2 步：测词法（8 个文件都要跑）

```bash
formatter.exe test/00_ident.c -t
formatter.exe test/01_variables.c -t
formatter.exe test/02_int.c -t
formatter.exe test/03_char.c -t
formatter.exe test/04_punct.c -t
formatter.exe test/05_full.c -t
formatter.exe test/06_float.c -t
formatter.exe test/07_nested.c -t
```

**判断标准**：每个文件都打印出单词表，末尾没有"词法错误"字样，就是通过。
重点看 `04_punct.c`：它会显示"按出现顺序"和"按种类编码排序"两段单词表，两段都要有。

### 第 3 步：测语法 + 格式化（7 个文件，00 除外）

```bash
formatter.exe test/01_variables.c
formatter.exe test/02_int.c
formatter.exe test/03_char.c
formatter.exe test/04_punct.c
formatter.exe test/05_full.c
formatter.exe test/06_float.c
formatter.exe test/07_nested.c
```

**判断标准**：每个文件都打印"语法分析结束"，并生成对应的 `_fmt.c` 文件。
打开 `05_full_fmt.c` 看：缩进整齐（4 空格一层），花括号对齐，运算符两侧有空格。

### 第 4 步：验证格式化输出是合法 C 代码

```bash
gcc -Wall -std=c11 -c test/05_full_fmt.c
```

**判断标准**：零警告零错误（说明格式化后代码还是合法 C）。

### 第 5 步：自测报错功能（重要！老师会随机改文件）

**词法报错**：临时写个带非法字符的文件测：

```bash
echo "a = @;" > test/bad.c
formatter.exe test/bad.c -t
```

应该看到：`词法错误 第X行: 非法字符`。

**语法报错**：临时写个缺分号的文件测：

```bash
echo "int a" > test/bad2.c
formatter.exe test/bad2.c
```

应该看到：`语法错误 第X行: 变量定义缺少分号`。

测完删掉临时文件。

---

## 五、验收现场操作步骤

老师会坐在旁边看你操作、提问。按这个顺序演示：

### 演示 1：词法分析（对应 50% 分）

1. 先编译一遍，让老师看到零警告。
2. 运行 `formatter.exe test/04_punct.c -t`
3. **指着屏幕解释**：
   - "这一段是按出现顺序显示每个单词（种类 + 值）"
   - "这一段是按种类编码排序显示（任务书加分项），同一种类去重"
   - 指出几个关键单词：`EQ ==`（双字符运算符）、`PREPROCESS #include`（预处理）、`COMMENT`（注释整段识别保留原文）

### 演示 2：语法分析 + AST（对应 40% 分）

1. 运行 `formatter.exe test/05_full.c`
2. 屏幕上先显示 AST（先根遍历带缩进），**指着树解释**：
   - "Program 是根，下面是各个函数定义和变量定义"
   - "这个 IfElse 结点有 3 棵子树：条件、if 子句、else 子句"
   - "这个 BinOp(+) 的左孩子是 sum，右孩子是 arr[i]"
3. 强调："**孩子兄弟表示法**存树，结点用 **union 共用体**区分类型"

### 演示 3：格式化输出（对应 10% 分）

1. 打开刚生成的 `test/05_full_fmt.c`
2. 说："这是对 AST 先根遍历生成的格式化文件，4 空格缩进、运算符两侧空格、花括号对齐"
3. （可选加分）说："这个文件再喂给程序还能解析，也能用 gcc 编译通过"

### 演示 4：报错功能（老师随机改文件时）

老师改文件后，你重新运行，程序要能：
- 报错 + 指出**行号**（词法错误第X行 / 语法错误第X行）

**你可以主动演示**："老师，我随便删个分号试试" → 删掉 05_full.c 里某行的 `;` → 运行 → 程序报"语法错误 第X行"。

---

## 六、老师可能问的问题（提前准备答案）

| 问题 | 怎么答 |
|---|---|
| 词法分析怎么做的？ | "用 DFA 状态机，gettoken 一次读一个单词，`前看一位 + ungetc 退回`识别双字符运算符" |
| 关键字怎么区分？ | "标识符拼完后查关键字表，命中返回关键字编码，否则 IDENT" |
| 语法分析用什么方法？ | "递归下降子程序法，每个语法成分一个子程序" |
| 表达式怎么处理优先级？ | "运算符优先关系表 + 双栈（操作数栈 + 运算符栈），借鉴数据结构表达式求值" |
| AST 怎么存的？ | "孩子兄弟表示法，child1 是第一个孩子，child2 是兄弟；结点用 union 共用体" |
| 怎么处理 if-else 悬挂问题？ | "递归下降里 if 的 then 分析完后看下一个是不是 else，是就归到 if-else 结点" |
| 注释和预处理怎么处理？ | "词法层识别成 COMMENT/PREPROCESS 单词，语法层截获建成 AST 结点（任务书 3.3.3(5)），格式化时原文写回" |
| 报错怎么带位置？ | "全局 line_no 计数器，每读一个换行 +1，报错时带上" |

---

## 七、验收打包清单（任务书 1.3(8) 要求）

最终提交打包（文件夹名：`专业班级-学号姓名`）：

- [ ] 源代码（6 个 .c + 5 个 .h）
- [ ] `formatter.exe`（可独立运行）
- [ ] `test/` 测试集（8 个测试源）
- [ ] 本验收指南（可作"操作手册"）
- [ ] 实验报告（纸质版 + 电子版）

---

## 八、当前程序状态确认（已测过）

- ✅ 编译：`gcc -Wall -std=c11` 零警告零错误
- ✅ 词法：8 个测试源 0 词法错误，含全部单词种类 + 排序显示
- ✅ 语法：7 个测试源全通过，覆盖全部 16 种语句/结构
- ✅ 格式化：7 个 `_fmt.c` 全部能 gcc 编译通过（合法 C）
- ✅ 报错：词法（非法字符）+ 语法（缺分号/缺括号）都带行号
