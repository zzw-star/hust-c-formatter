# 验收问答准备（助教可能问的问题 + 标准答案）

> 按分类整理，每题都给了"该怎么答"。**答案里的说法都和你的代码实现一致**，可以直接照着说。
> 建议：先理解再背，助教追问时能接得住。

---

## 一、整体设计类（几乎必问）

### Q1：你的程序整体是怎么设计的？分了几个模块？

**答**：

> 按任务书 4.2 的"模块化"要求，我把程序分成 6 个源文件：
>
> | 文件 | 职责 |
> |---|---|
> | `main.c` | 主控：解析命令行参数，调度各阶段 |
> | `lexer.c` | 词法分析：读源文件，识别出一个个单词 |
> | `parser.c` | 语法分析：递归下降，生成抽象语法树 |
> | `ast.c` | 语法树：结点建立、先根遍历显示、释放 |
> | `printer.c` | 格式化输出：遍历 AST 生成缩进编排的 `.c` 文件 |
> | `error.c` | 统一报错出口，维护错误计数 |
>
> 每个模块配一个 `.h` 头文件声明接口，模块之间通过头文件调用，不会互相包含实现。

---

### Q2：程序的处理流程是什么？

**答**：

> 三个阶段，串起来的：
>
> 1. **词法分析**：`gettoken()` 每次读一个单词，返回"种类编码"，单词自身值存在全局 `token_text` 里
> 2. **语法分析**：递归下降分析单词序列，同时建抽象语法树
> 3. **格式化输出**：对 AST 先根遍历，按缩进规范写到 `.c` 文件
>
> 命令行加 `-t` 时只做第 1 步（词法模式）；不加 `-t` 就跑完整的三步。

---

### Q3：为什么用 `token_text` 全局变量存单词值？

**答**：

> 这是指导书里明确给的写法。因为有些单词光有"种类编码"不够——比如识别出 `IDENT`，还得知道这个标识符具体叫什么（`abc` 还是 `i`）；识别出 `INT_CONST`，还得知道值是 `123` 还是 `0x123`。
>
> 所以单词有两个特征值：**种类编码**（返回值）+ **自身值**（存在 `token_text` 全局字符数组里）。

---

## 二、词法分析类（50% 分值，问得最多）

### Q4：词法分析是怎么实现的？

**答**：

> 用的是**有穷自动机（DFA）**的思路。核心函数 `gettoken()` 每次被调用就识别一个单词，处理流程是：
>
> 1. **过滤空白**：空格、制表符、回车换行跳过；遇到换行 `line_no++`（为报错定位做准备）
> 2. **跳过注释和预处理**：这个必须放在最前面，否则 `/` 会被当成除号
> 3. **判断入口字符**：
>    - 字母/下划线 → 拼标识符，拼完查关键字表
>    - 数字 → 拼数字（整数/浮点/十六进制）
>    - 单引号 → 字符常量
>    - `=` `!` `<` `>` `&` `|` → 可能是双字符运算符
>    - 其他 → 查单字符运算符和定界符表
>    - 都不匹配 → 报"非法字符"
>
> 每识别出一个单词就 `return` 对应的种类编码。

---

### Q5：关键字是怎么识别的？

**答**：

> 关键是：**关键字和标识符的拼法规则一样**，所以先按标识符拼出来，再去查表。
>
> 我建了一个关键字查找表（结构体数组），里面有 10 个关键字：`int` `float` `char` `if` `else` `while` `for` `return` `break` `continue`。
>
> 拼完标识符后遍历这张表，`strcmp` 比对：
> - 命中 → 返回该关键字的种类编码（比如 `int` 返回 `INT`）
> - 没命中 → 返回 `IDENT`（普通标识符）
>
> 这样加新关键字只要往表里加一行，不用改判断逻辑。

---

### Q6：`==` 这种双字符运算符怎么识别？会不会把 `a = 1` 的 `=` 误判？

**答**：

> 用**前看一位**的方法，这也是指导书伪代码里给的标准写法。
>
> 读到 `=` 时，先 `fgetc` 再读一个字符：
> - 如果下一个还是 `=` → 拼成 `==`，返回 `EQ`
> - 如果不是（比如 `a = 1` 里 `=` 后面是空格）→ 用 `ungetc` 把多读的字符**退回到输入缓冲区**，返回 `ASSIGN`
>
> `ungetc` 是关键——多读的字符不能丢，退回去后下次 `gettoken` 还能读到它。
>
> 同样的套路处理 `!=` `<=` `>=` `&&` `||`。

---

### Q7：整型常量有几种形式？你的种类编码怎么区分？

**答**：

> 任务书里明确说了：**以类型不同作为划分种类编码的标准**。
>
> | 形式 | 类型 | 编码 |
> |---|---|---|
> | `123` | int | `INT_CONST` |
> | `0123`（八进制） | int | `INT_CONST` |
> | `0x123`（十六进制） | int | `INT_CONST` |
> | `123L` | long | `LONG_CONST` |
> | `1.5` `.5` `1.` `1.5F` | float | `FLOAT_CONST` |
>
> 关键点：**`0x123` 和 `123` 都是 int，编码相同；但 `123L` 是 long，编码必须不同**。
>
> 实现上：先拼数字串，如果开头是 `0` 且下一个是 `x`，就按十六进制继续吃 `0-9a-f`；然后看有没有小数点（→ 浮点）；最后看后缀 `F`（→ 浮点）或 `L`（→ 长整型）。

---

### Q8：注释和预处理是怎么处理的？

**答**：

> 这两类**不属于语法成分**，由词法层识别、语法层截获建树：
>
> - **行注释 `//`**：读到就一直吞到换行符，换行不吞（留给空白处理逻辑，顺便让行号 +1），整段文本返回 `COMMENT` 单词
> - **块注释 `/* */`**：吞到 `*/` 结束，**支持跨行**，跨行时每遇到一个换行就 `line_no++`（否则后面报错的行号会全错），整段文本返回 `COMMENT` 单词
> - **预处理 `#`**：整行吞掉，**返回 `PREPROCESS` 编码**，整行文本存在 `token_text` 里
>
> 为什么返回编码而不是直接跳过？因为任务书 3.3 把"编译预处理"和"注释"列为要识别的单词种类，且 3.3.3(5) 要求**生成的 AST 包含编译预处理和注释**。所以语法层的 advance() 读到这两类单词时把它们建成 `NODE_PREPROCESS`/`NODE_COMMENT` 结点存入待挂链，再由 program()/Compound() 挂到 AST 对应层上——语法分析流程完全不受干扰，格式化输出时原文整段写回。

---

### Q9：词法报错是怎么定位的？

**答**：

> 用一个全局行号计数器 `line_no`：词法层每读到一个换行符就 `+1`，块注释跨行时也照样计数。
>
> 遇到不符合单词定义的符号时，调用 `lex_error(line_no, "非法字符")`，打印出来就是：
> ```
> 词法错误 第2行: 非法字符
> ```
>
> 任务书说"检查时由老师随机修改测试文件，设置错误，检查报错功能"——所以我的测试文件里不放错误，但程序随时能报错。

---

## 三、语法分析类（40% 分值）

### Q10：语法分析用什么方法？

**答**：

> **递归下降子程序法**（任务书 3.2 建议的方法）。
>
> 基本思想是：**每个语法成分对应一个子程序**。比如：
> - `program()` 处理整个程序
> - `ExtDef()` 处理一个外部定义
> - `FuncDef()` 处理函数定义
> - `Compound()` 处理复合语句
> - `Statement()` 处理一条语句
> - `parse_expr()` 处理表达式
>
> 有个重要约定：**调用子程序时，该语法成分的第一个单词已经读入了全局 `w_kind`**，子程序处理完刚好读到下一个语法成分的第一个单词，方便后续处理。

---

### Q11：if-else 的"悬空 else"问题你怎么解决？

**答**：

> 递归下降**天然解决**了这个问题。
>
> `IfStmt()` 的流程是：
> 1. 吃掉 `if`，匹配 `(`，解析条件表达式，匹配 `)`
> 2. 递归调用 `Statement()` 处理 then 分支
> 3. **立刻看下一个单词是不是 `else`**：
>    - 是 `else` → 再递归调用 `Statement()` 处理 else 分支，生成 `NODE_IF_ELSE`（3 棵子树）
>    - 不是 → 生成 `NODE_IF`（2 棵子树），结束
>
> 因为处理是**递归**的，最内层的 if 会先完成自己的 else 配对，所以 `else` 自然归给**最近的、还没配对的 if**，符合 C 语言的语义。

---

### Q12：表达式是怎么处理的？优先级怎么保证？

**答**：

> 用的是指导书推荐的**运算符优先 + 双栈**算法，借鉴数据结构课的表达式求值，但**不求值，只建树**。
>
> - **操作数栈**：存已经建好的子树根指针（标识符、常量、函数调用…）
> - **运算符栈**：存运算符，栈底放一个 `#` 作为起止标记
>
> 流程：
> 1. 读到操作数 → 建叶子结点，压操作数栈
> 2. 读到运算符 → 和栈顶运算符比较优先级：
>    - 栈顶优先级**低** → 当前运算符压栈
>    - 栈顶优先级**高** → **归约**：运算符出栈，操作数栈弹出两个（右操作数、左操作数），建一个运算符结点，再把新结点压回操作数栈
>    - 重复比较直到可以压栈
> 3. 遇到结束符（`;` `)` `]`）→ 把栈里剩余运算符全部归约
>
> 比如 `1+2*3`：读到 `*` 时，栈顶是 `+`，`+` 优先级低于 `*`，所以 `*` 压栈；最后归约顺序是 `2*3` 先，再 `1+(2*3)`，结果正确。

---

### Q13：赋值 `=` 是右结合的，你怎么处理的？

**答**：

> 运算符的结合性我用**优先级数值 + 结合性标记**来实现：
> - 左结合运算符：栈顶优先级 `>=` 当前 → 归约
> - **右结合运算符（只有 `=`）**：栈顶优先级 `>` 当前 → 归约（即**同级不归约，继续压栈**）
>
> 这样 `a = b = 1` 处理时，第二个 `=` 遇到栈顶的 `=`，因为同级不归约，所以压栈，最后从右往左归约，形成 `a = (b = 1)`，符合右结合。
>
> 这个实现和指导书的"运算符优先关系表"是等价的，只是用数值表达更直观。

---

### Q14：括号、函数调用、数组下标怎么处理的？

**答**：

> 我把这三类作为**"因子"**用递归处理，而不是塞进运算符栈——这样更简单可靠。
>
> `read_factor()` 读一个因子时：
> - 读到标识符后**再看下一个单词**：
>   - 是 `(` → **函数调用**，递归解析实参列表（逗号分隔），建 `NODE_FUNC_CALL`
>   - 是 `[` → **数组下标**，递归解析下标表达式，建 `NODE_ARRAY_INDEX`
>   - 都不是 → 普通标识符，建 `NODE_IDENT`
> - 读到 `(` → **括号表达式**，递归调用 `parse_expr(RP)` 解析括号内内容
>
> 因为函数调用和数组下标本身就是**优先级最高**的语法成分，放在因子层递归处理，语义和放进运算符栈是等价的。

---

### Q15：嵌套结构（if 套 if、循环套循环）怎么支持的？

**答**：

> 递归下降**天然支持嵌套**，不需要额外处理。
>
> 比如 `Compound()`（复合语句）里循环调用 `Statement()`，而 `Statement()` 遇到 `if` 会调用 `IfStmt()`，`IfStmt()` 的 then 分支又是一个 `Statement()`——这样就形成了递归，嵌套多少层都能处理。
>
> 我的测试用例 `07_nested.c` 里就有：双重 `for` 循环嵌套、if 里套 if、`while` 里套 `if`，都能正确建树。

---

### Q16：语法报错的位置怎么确定的？

**答**：

> 这里我做了个**精细处理**。一开始我直接用当前行号，发现有问题：
>
> 如果第 1 行末尾少了分号，错误要读到第 2 行的单词才会暴露，结果报"第 2 行"——**位置差了一行**。
>
> 所以我加了两个变量：
> - `w_line`：当前 token 所在行
> - `prev_line`：**上一个 token** 所在行
>
> 在 `advance()` 里维护：读新 token 前先把旧的 `w_line` 存到 `prev_line`。
>
> 然后"缺少分号"这类**期望某符号却没出现**的错误，报 `prev_line`（错误发生在上一个 token 之后）；这样删第 1 行的分号就准确报"第 1 行"。

---

## 四、抽象语法树类

### Q17：AST 是怎么存储的？

**答**：

> 用的是**孩子兄弟表示法**（指导书里提到的两种之一）：
>
> ```c
> typedef struct ASTNode {
>     NodeTag tag;            // 结点种类
>     union { ... } attr;     // 各结点专属属性
>     struct ASTNode *child1; // 第一个孩子
>     struct ASTNode *child2; // 下一个兄弟
> } ASTNode;
> ```
>
> - `child1` 指向**第一个孩子**
> - `child2` 指向**下一个兄弟**（不是第二个孩子）
>
> 这样一个结点可以有任意多个孩子：从 `child1` 开始，沿着 `child2` 一路串下去。
>
> 我写了个 `add_child(parent, child)` 函数统一追加孩子，保证所有结点的建树方式一致。

---

### Q18：结点种类那么多，怎么区分和访问属性？

**答**：

> 用**枚举 tag + union 共用体**，这也是指导书明确要求的。
>
> - `NodeTag` 枚举定义了 29 种结点（`NODE_PROGRAM`、`NODE_IF_ELSE`、`NODE_BINOP`、`NODE_IDENT`…）
> - `union` 里按结点种类存放各自的属性：
>   - 标识符 → `ident.name`（名字字符串）
>   - 整型常量 → `int_const.value`（数值）+ `int_const.text`（字面量）
>   - 类型 → `type.name`（"int"/"float"/"char"）
>   - 双目运算 → `binop.op`（运算符编码）
>
> 访问时用 `switch(node->tag)` 判断种类，再取对应的 union 成员，不会取错。

---

### Q19：AST 是怎么显示的？

**答**：

> **先根遍历 + 缩进打印**（任务书要求）。
>
> `print_tree(root, depth)` 递归：
> 1. 按 `depth` 打印缩进（每层 2 个空格）
> 2. 根据 `tag` 打印结点名和属性，比如 `Ident(sum)`、`IntConst(10)`、`BinOp(+)`
> 3. 递归打印 `child1`（孩子，**深度 +1**）
> 4. 递归打印 `child2`（兄弟，**深度不变**）
>
> 兄弟深度不变这点很重要——否则同一层的语句缩进会越缩越深。

---

## 五、格式化输出类

### Q20：格式化用的是什么风格？

**答**：

> 用的 **Allman 风格**（任务书说"自行认定一种规范"）：
>
> - 缩进 **4 个空格**，每进一层 +4
> - 函数和复合语句的**花括号独占一行**
> - **二元运算符两侧各一个空格**（`a + b` 而不是 `a+b`）
> - 顶层定义之间空一行
>
> 实现上，`format_source()` 先根遍历 AST，每个结点类型对应一段输出逻辑，用一个全局 `indent` 控制缩进层级。

---

### Q21：怎么保证格式化后的代码和原来语义一样？

**答**：

> 我做了两层验证：
>
> 1. **往返验证**：格式化输出的文件再喂给程序，还能成功解析（说明 AST 结构没丢）
> 2. **gcc 编译验证**：用 `gcc -Wall -std=c11 -c test\05_full_fmt.c` 编译格式化结果，**零警告通过**，说明生成的是合法 C 代码

---

### Q22：（加分点）格式化会改变常量的写法吗？

**答**：

> **不会。我特意保留了字面量原样。**
>
> 一开始我踩了个坑：常量在 AST 里只存数值，格式化时用 `%d`/`%g` 输出，结果：
> - `0xFF` 变成了 `0`（`atoi` 不认十六进制）
> - `1.` 变成了 `1`（`%g` 去掉了小数点，浮点变整型）
>
> 这样格式化后**语义被破坏了**。
>
> 修复办法：给常量结点加了一个 `text[64]` 字段**存原始字面量**，格式化时直接输出 `text`。
>
> 现在 `0xFF` 还是 `0xFF`，`1.` 还是 `1.`，`.5` 还是 `.5`，`123456789L` 的 `L` 后缀也保留。数值字段也用 `strtol(...,0)` 修正了（`0` 表示自动识别十六进制和八进制）。

---

## 六、测试类

### Q23：你的测试用例是怎么设计的？

**答**：

> 按任务书 3.4"用例未能反映的功能视同未实现"的要求，我准备了 **8 个测试源**，保证覆盖全部功能点：
>
> | 文件 | 覆盖内容 |
> |---|---|
> | `00_ident.c` | 标识符、全部关键字 |
> | `01_variables.c` | 变量、数组、函数声明/定义、if-else、函数调用 |
> | `02_int.c` | 整型常量 4 种形式（123/0123/0x123/123L） |
> | `03_char.c` | 字符常量 4 种（'a'/'X'/'\n'/'\0'） |
> | `04_punct.c` | **全部运算符 + 定界符 + 注释 + 预处理** |
> | `05_full.c` | 全部 12 种语句 + 函数 + 数组 + 嵌套 |
> | `06_float.c` | 浮点常量 4 种（1.5/.5/1./1.5F） |
> | `07_nested.c` | if 嵌套、循环嵌套、递归调用 |
>
> 覆盖了任务书要求的 16 种语法结构：表达式语句、if、if-else、while、for、return、break、continue、复合语句、函数定义、函数声明、函数调用、if 嵌套、循环嵌套、外部变量说明、局部变量说明。

---

### Q24：报错功能怎么演示？

**答**：

> 任务书说测试文件里不必包含错误，由老师随机改。所以我可以现场演示：
>
> - **词法错误**：在源文件里加个 `@` → 程序报 `词法错误 第X行: 非法字符`
> - **语法错误**：删掉某个分号 → 程序报 `语法错误 第X行: 变量定义缺少分号`
> - **括号不匹配**：删掉右括号 → 报 `语法错误 第X行: 表达式语法错误（缺少右括号）`
>
> 行号是精确的（用 `prev_line` 定位）。

---

## 七、特色与不足（设计特色 10%，加分题）

### Q25：你的程序有什么特色/创新点？

**答**（挑 2-3 个说）：

> 1. **常量字面量原样保留**：格式化不改 `0xFF`、`1.`、`.5` 的写法，保证语义完全一致（很多实现会在这出错）
> 2. **报错位置精确到行**：用 `prev_line` 修正了"缺分号报下一行"的经典问题
> 3. **表达式双栈 + 因子递归混合**：括号、函数调用、数组下标用递归处理，比纯双栈处理括号更清晰可靠
> 4. **单词按种类编码排序显示**（任务书加分项）：识别完所有单词后按编码排序并去重，方便检查覆盖度
> 5. **中文输出乱码处理**：程序主动切换控制台到 UTF-8，在任何 Windows 电脑上都能正常显示

---

### Q26：你的程序有什么不足？可以怎么改进？

**答**（诚实说，但要有改进方向）：

> 目前主要的限制：
>
> 1. **语言子集有限**：只支持 `int/float/char`，不支持指针、结构体、枚举（这是任务书的要求范围）
> 2. **没有单目运算符**：不支持 `-1`、`!a` 这类（任务书明确说不包含单目运算符）
> 3. **没有语义检查**：只做语法分析，不检查变量是否声明、类型是否匹配
> 4. **预处理只识别不展开**：`#define N 10` 只是整行识别，不会真的做宏替换
>
> 如果要改进，下一步可以做：宏替换、类型检查、支持更多 C 语法（`switch`、`do-while`、指针）。

---

## 八、验收时的加分小技巧

1. **主动演示报错**：别等老师改，你说"老师我可以演示一下报错功能"——显得准备充分
2. **主动说验证方式**：提到"格式化结果能用 gcc 编译通过"，证明输出是合法代码
3. **主动指出存储方式**：说"孩子兄弟表示法 + union 共用体"，这是任务书明确要求的
4. **主动说踩过的坑**：比如"我一开始格式化把 0xFF 变成 0，后来加了字面量字段修复"——老师喜欢听真实的调试过程
5. **测试文件命名清晰**：老师一看 `04_punct.c`（运算符）、`07_nested.c`（嵌套）就知道覆盖度

---

## 附：高频问题速查（万一紧张，记这几句）

| 老师问 | 一句话答 |
|---|---|
| 词法怎么做的？ | DFA 状态机，`gettoken` 一次读一个单词，返回种类编码 + `token_text` 存值 |
| 关键字怎么认？ | 先按标识符拼，再查关键字表，命中返回关键字编码 |
| `==` 怎么认？ | 前看一位 + `ungetc` 退回 |
| 语法什么方法？ | 递归下降子程序法，每个语法成分一个子程序 |
| 表达式优先级？ | 运算符优先 + 双栈（操作数栈 + 运算符栈） |
| else 配对？ | 递归下降天然解决，else 归最近的 if |
| AST 怎么存？ | 孩子兄弟表示法（`child1` 孩子 + `child2` 兄弟）+ union 共用体 |
| 格式化什么风格？ | Allman，4 空格缩进，花括号独占一行 |
| 报错怎么定位？ | 全局 `line_no` + `prev_line` 修正 |
